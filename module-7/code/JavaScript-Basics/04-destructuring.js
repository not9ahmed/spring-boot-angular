const point = [100, 200];

const [x, y] = point;

console.log(`x: ${x}, y: ${y}`);


const person = {
    name: "Ahmed",
    age: 25,
    hobby: "Programming"
}

const {name, age, hobby} = person;

console.log(name);
console.log(age);
console.log(hobby);